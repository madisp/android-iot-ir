package pink.madis.things.ir;

/**
 * A "handle" that can do things, emit IR codes, ping devices, etc.
 */
public class RemoteContext {
    public boolean ping(String ip) {
        // check if IP is alive
        return false;
    }
}
